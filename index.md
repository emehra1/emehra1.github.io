---
# To change the default theme layout see: https://jekyllrb.com/docs/themes/#overriding-theme-defaults
# All page content is driven by `content:` in _config.yml and rendered by
# _layouts/default.html, which never emits {{ content }}. This file exists only
# to route `/` through the default layout, so a body here would not render.
layout: default
---
